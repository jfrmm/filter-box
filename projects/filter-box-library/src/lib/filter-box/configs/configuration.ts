import { FilterBoxConfig } from '../../../public-api';

export const configuration: FilterBoxConfig = {
  buttons: {
    clearAll: 'simple',
  },
  flex: {
    gap: '24px',
    offset: {
      left: '40px',
    },
  },
};
