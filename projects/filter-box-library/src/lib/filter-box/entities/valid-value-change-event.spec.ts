import { ValidValueChangeEvent } from './valid-value-change-event';

describe('ValidValueChangeEvent', () => {
  it('should create an instance', () => {
    expect(new ValidValueChangeEvent()).toBeTruthy();
  });
});
