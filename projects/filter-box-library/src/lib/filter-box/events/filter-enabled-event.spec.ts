import { FilterEnabledEvent } from './filter-enabled-event';

describe('FilterEnabledEvent', () => {
  it('should create an instance', () => {
    expect(new FilterEnabledEvent()).toBeTruthy();
  });
});
