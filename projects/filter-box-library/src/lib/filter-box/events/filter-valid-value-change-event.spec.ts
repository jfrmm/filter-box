import { FilterValidValueChangeEvent } from './filter-valid-value-change-event';

describe('ValidValueChangeEvent', () => {
  it('should create an instance', () => {
    expect(new FilterValidValueChangeEvent()).toBeTruthy();
  });
});
