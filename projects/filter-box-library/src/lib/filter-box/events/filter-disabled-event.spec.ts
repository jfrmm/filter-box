import { FilterDisabledEvent } from './filter-disabled-event';

describe('FilterDisabledEvent', () => {
  it('should create an instance', () => {
    expect(new FilterDisabledEvent()).toBeTruthy();
  });
});
