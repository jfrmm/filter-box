import { FilterAnchorDirective } from './filter-anchor.directive';

describe('FilterAnchorDirective', () => {
  it('should create an instance', () => {
    // const directive = new FilterAnchorDirective();
    // expect(directive).toBeTruthy();
  });
});
