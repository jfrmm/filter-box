import { FilterBoxConfig } from '../lib/filter-box/models/filter-box-config.model';

export const configuration: FilterBoxConfig = {
  buttons: {
    clearAll: 'simple',
  },
  flex: {
    gap: '24px',
    offset: {
      left: '40px',
    },
  },
};
